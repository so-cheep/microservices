import { pusherApiGatewayCore } from './pusher-api-gateway-core';

describe('pusherApiGatewayCore', () => {
  it('should work', () => {
    expect(pusherApiGatewayCore()).toEqual('pusher-api-gateway-core');
  });
});
